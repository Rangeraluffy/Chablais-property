<!-- ======= Header/Navbar ======= -->
<nav class="navbar navbar-default navbar-trans navbar-expand-lg">
  <div class="container">
    <% if account_signed_in? %>
      <%= link_to "Dashboard", dashboard_path,class: "nav-link" %>
    <% else %>
      <a class="nav-link" href="<%= account_signed_in? ? dashboard_path : root_path %>">Peillex Real Estate</a>
    <% end %>

    <div class="navbar-collapse collapse justify-content-center" id="navbarDefault">
      <ul class="navbar-nav">

        <li class="nav-item">
          <%= link_to(translate('home'), root_path,class: "nav-link") %>
        </li>

        <li class="nav-item">
          <%= link_to(translate('properties'), filter_properties_path ,class: "nav-link") %>
        </li>

        <li class="nav-item">
          <%= link_to "Blog", blog_path,class: "nav-link" %>
        </li>

        <li class="nav-item">
          <%= link_to(translate('contact'), contact_contact_path,class: "nav-link" ) %>
        </li>
      </ul>
      <ul class="navbar-nav">
        <% if account_signed_in? %>
          <li class="nav-item">
            <%= link_to(translate('logOut'), destroy_account_session_path, method: :delete, class: "nav-link") %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to(translate('logIn'), new_account_session_path, class: "nav-link") %>
          </li>
          <li class="nav-item">
            <%= link_to(translate('signUp'), new_account_registration_path, class: "nav-link") %>
          </li>
        <% end %>
      </ul>
    </div>
    <ul class="navbar-nav">
      <li class="nav-item">
        <div class="row">
          <ul class="languages-nav">
            <% I18n.available_locales.each do |locale| %>
              <li><%= link_to locale, url_for(locale: locale), class: "nav-link" %></li>
            <% end %>
          </ul>
        </div>
      </li>
    </ul>
  </div>
</nav>
<!-- End Header/Navbar -->
