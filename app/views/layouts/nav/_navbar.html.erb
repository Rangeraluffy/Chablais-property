<div class="container">
  <nav class="navbar navbar-expand-md fixed-top">
    <div class="collapse navbar-collapse" id="navbarsExampleDefault">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item">
          <% if account_signed_in? %>
            <%= link_to "Dashboard", dashboard_path,class: "nav-link" %>
          <% else %>
            <a class="nav-link" href="<%= account_signed_in? ? dashboard_path : root_path %>">Peillex Real Estate</a>
          <% end %>
        </li>
        <li class="nav-item">
          <%= link_to "Blog", blog_path,class: "nav-link" %>
        </li>
      </ul>
      <ul class="navbar-nav mr-auto">
        <% if account_signed_in? %>
          <li class="nav-item">
            <%= link_to "Log out", destroy_account_session_path, method: :delete, class: "nav-link" %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "Log In", new_account_session_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "Sign Up", new_account_registration_path, class: "nav-link" %>
          </li>
        <% end %>
      </ul>
    </div>
  </nav>
</div>
